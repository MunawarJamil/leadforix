[project]
name = "leadforix"
version = "0.1.0"
description = "Leadforix AI Sales Platform"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.116.1",
]

[tool.uv]
package = false